# 俱乐部界面改版 — 对齐扯旋实施方案

> 调研来源:`E:\cocos\club\assets`(扯旋俱乐部 Cocos 前端)+ `E:\cocos\chexuan-springboot`(主服接口)。
> 原则:**元素/逻辑对齐扯旋,视觉风格用我们自己的**(白底薄荷绿,cx-dzpk-pixi 现有风格)。
> 金额单位一律"分",显示 ÷100。

---

## 总览:改动清单与顺序

| # | 改动 | 前端 | 后端 | 状态 |
|---|------|------|------|------|
| 1 | 5 tab 修复来回切换;第二 tab=小游戏 | HallBottomBar 统一路由;新增 小游戏/生涯/我的 页 | 无 | ✅ 已完成 |
| 2 | 好友局页:钻石卡下方 = 俱乐部列表 | FriendView 重构 | 无(CLUB_LIST 已够) | 本轮 |
| 3 | 俱乐部详情:顶部对齐扯旋 + 牌局列表好友局样式 | ClubDetailView 重构 | 无 | 本轮 |
| 4 | 左下角「战绩」「俱乐部」两个底部弹框 | 新组件 ClubRecordSheet / ClubSettingSheet | MY_RECORDS 加 clubId 过滤 | 本轮 |
| 5 | 设置面板(成员/合伙人/积分明细/统计/设置) | ClubSettingSheet 内多 Tab | 新增 CLUB_UPDATE(改资料) | 本轮 |
| 6 | 顶栏消息弹框(俱乐部待审) | HallTopBar 加消息按钮+弹框 | CLUB_APPLY_LIST 支持 clubId=0 聚合 | 本轮 |

---

## 1. 底部 5 tab(✅ 已完成)

扯旋大厅底栏五 tab;我们对应:**大厅 / 小游戏 / 好友局 / 生涯 / 我的**。
- 路由统一收进 `HallBottomBar`(点击自跳转),修复"来回点不动"。
- 小游戏 = 占位页(后续接钻石小游戏);生涯 = 完赛记录页(411);我的 = 账号信息+退出登录。
- 旧「俱乐部」独立 tab 取消,俱乐部列表并入好友局页(见 §2)。

## 2. 好友局页(FriendView 重构)

扯旋结构:顶部操作卡 → 钻石小游戏/钻石大厅卡 → **俱乐部列表**。

本次布局(从上到下):
1. 公用顶栏(HallTopBar,含消息按钮 §6)
2. 三张操作卡:创建牌局(须先选俱乐部,无俱乐部提示创建)/ 加入牌局(输房间号)/ 俱乐部(创建+加入按钮,即原 ClubView 弹窗逻辑搬过来)
3. 钻石小游戏 / 钻石大厅 两卡(保留占位)
4. **俱乐部列表**(对齐扯旋 `getAllMyClubs` + ClubListItem):
   - 数据:WS 421 `CLUB_LIST`(已返回 clubId/clubNo/name/remark/avatar/notice/myRole/memberCount/pendingCount/myInviteCode)
   - 列表项元素(对齐扯旋):头像、`名称(编号)`、`简介:xxx`、**创建者标识**(myRole=3 显示"群主"角标;扯旋只标创建者,我们加全角色角标更实用:群主绿/管理紫/合伙人黄/成员灰)、成员数、待审红点(pendingCount>0)
   - 点击 → `/club/{id}` 俱乐部详情
5. 假数据牌局列表删除;世界杯 banner 删除。

旧 `/club` 列表页(ClubView)废弃,创建/加入弹窗迁入 FriendView。

## 3. 俱乐部详情页(ClubDetailView 重构)

对齐扯旋 `ClubMainView`:

**顶部(ClubTop)**:
- 左:返回按钮 + **俱乐部头像**(扯旋这里放玩家头像,属历史怪癖;我们放俱乐部头像更合理)+ `俱乐部名 (编号)` + 角色角标(对齐 `ClubRoleUtil.getBadgeLevel`:成员/合伙人/群主·管理/)
- 右:**我的俱乐部积分** `memberScore`(formatGold)
- 公告 `notice`:跑马灯条(有公告时显示,水平滚动)
- 建房按钮:仅群主/管理员(对齐 canCreateGame:合伙人不可建房)→ 跳 `/create-room/:clubId`

**中间:牌局列表(样式对齐好友局 room item)**:
- 数据:WS 402 `ROOM_LIST {clubId}`(已有)
- 列表项元素(对齐扯旋 ClubTableItem + 好友局样式):序号、房间号 `#roomId`、牌局名、盲注 `sb/bb`、时长 `settleTimeMins分钟`、思考时间、状态(等待中/进行中)、入座 `seated/maxPlayers`、已坐玩家头像列(快照座位)
- 群主/管理员对空桌显示「解散」;点击进桌。

**左下角两个菜单(AT_Bottom)**:「战绩」+「俱乐部」,点开从底部弹出(bottom sheet,§4/§5)。

## 4. 「战绩」底部弹框(ClubRecordSheet)

对齐扯旋 `CHEXUANRecordPanel`(`POST /user/gameSummary {clubId}`):

- 顶部汇总:胜/负/平局次数、总手数、总局数(dzpk 现有 stats:sessions/totalHands/totalProfit,补 win/lose 场次)
- 列表项:房间号、起止时间 `MM.dd hh:mm`、带入、手数、战绩(正绿负红)、服务费、保险(对应扯旋奖池位)
- 无日期 Tab,分页/加载更多。

后端:`MY_RECORDS(411)` 增加可选 `clubId` 过滤(dz_session_record 需有 club_id 字段,没有则加列)。

## 5. 「俱乐部」底部弹框(ClubSettingSheet,逻辑最多)

对齐扯旋 `ClubSettingPanel`:顶栏显示 `专属ID {myInviteCode}` + 复制;Tab 按角色出(扯旋按后端 menus[],我们按角色矩阵等价实现):

| Tab | 可见角色 | 内容 | 接口(dzpk WS) |
|-----|---------|------|----------------|
| 成员管理 | 全部 | 成员列表+操作气泡 | 425 CLUB_MEMBERS |
| 合伙人 | 群主/管理/合伙人 | role=4 列表+分成比例 | 425 过滤 role=4 |
| 积分明细 | 全部 | 我的积分流水(type 着色) | 431 CLUB_SCORE_LOGS |
| 俱乐部设置 | 群主/管理员 | 改头像/名称/简介/公告 | **新增 433 CLUB_UPDATE** |

**成员列表项**(对齐 ClubMenberListItem):头像、昵称、`ID:numberId`、角色标签(颜色:成员白/管理紫/群主绿/合伙人黄)、积分、在线状态。

**操作气泡**(对齐扯旋权限矩阵,dzpk 已有接口):
- 上下分/增发(群主对自己=增发`ownerAdd`/`ownerBurn`;群主管理对他人=上分`distribute`/勾选回收`collect`;成员/合伙人=赠送`transfer`)→ 430
- 积分记录(查任意成员)→ 431 带 userId
- 设/取消管理员(仅群主)、设/取消合伙人(带比例,只升不降)→ 426
- 踢出(群主/管理任意,合伙人仅直推)→ 427
- 退出俱乐部(非群主)→ 428;解散(仅群主)→ 429
- 快捷金额 50/100/500/1000/5000(×100 分)

**扯旋有但本次不做**(后续按需):禁言/冻结、贡献/下级钻取、合伙人佣金统计+提取(dzpk 佣金随周期结算自动分,无需手动提取)、联盟、俱乐部日统计。

## 6. 顶栏消息弹框(俱乐部消息)

对齐扯旋:大厅 `AT_NoticeBtn` + 红点 `CLUB_JOIN` → `ClubNoticePanel`(`getAllMyClubsJoinRequests`,status=0 待审)。

- HallTopBar 右侧加「消息」铃铛按钮,红点数 = 我管理的俱乐部待审总数(CLUB_LIST 的 pendingCount 求和,登录后拉一次+CLUB_NOTIFY 推送时刷新)
- 点开底部弹框:待审申请列表,项元素对齐扯旋 ClubNoticeListItem:申请人头像、昵称、`ID:numberId`、时间 `yyyy-MM-dd hh:mm:ss`、俱乐部名、「同意」「拒绝」按钮
- 同意/拒绝 → 424 CLUB_REVIEW;操作后红点减一。

后端:`CLUB_APPLY_LIST(423)` 支持 `clubId=0` = 聚合我(群主/管理员)所有俱乐部的待审申请,返回项带 clubId/clubName。

## 7. 后端新增/修改汇总

| 消息 | 改动 |
|------|------|
| 423 CLUB_APPLY_LIST | clubId=0 聚合全部我管理的俱乐部待审 |
| 411 MY_RECORDS | 加可选 clubId 过滤 + stats 补 winSessions/loseSessions |
| **433 CLUB_UPDATE / 490 CLUB_UPDATE_RES** | 新增:改俱乐部 头像/名称/简介/公告(群主/管理员;校验同创建) |
| dz_session_record | 若无 club_id 列则加列(记录房间所属俱乐部) |

## 8. 角色/枚举对照(与扯旋一致)

- 角色:1 成员 / 2 管理员 / 3 群主 / 4 合伙人
- 申请 status:0 待审 / 1 通过 / 2 拒绝
- 积分流水 type:1 坐下带入 / 2 起立返还 / 3 每局结算 / 10·16 上分 / 11·17 下分 / 12·13 赠送 / 14 增发 / 15 提红利 / 18 礼物 / 19·20 逃跑罚 / 21 核销
- 权限硬规则:合伙人不能建房不能审批;管理员不能增发;解散仅群主;设合伙人仅直推。
