# cx-dzpk 部署文档(生产)

> 服务器:47.122.115.33(root,凭据在本机 `C:\Users\admin\.cursor\projects\e-cocos\deploy-credentials.local.md`,不入库)
> 部署目录:`/home/fzcx/cx-dzpk` —— **每个子游戏单独一个文件夹**,与扯旋 `/home/fzcx/chexuan` 平级
> 容器:`cx-dzpk`(宿主 **19100** → 容器 9100),独立 compose 项目,**部署/重启不影响扯旋**

## 一、环境速览

| 项 | 值 |
|----|-----|
| 部署目录 | `/home/fzcx/cx-dzpk` |
| 容器 | `cx-dzpk`(宿主 19100 → 容器 9100) |
| 基础镜像 | `eclipse-temurin:17-jre-alpine`(**服务器已有**,勿换需外网拉取的镜像——国内拉 Docker Hub 会挂死) |
| 依赖 | 无(内存态,不用 MySQL/Redis) |
| 联调页 | `http://47.122.115.33:19100/`(需安全组放行 TCP 19100) |
| WS 端点 | `ws://47.122.115.33:19100/ws/dzpk` |
| 日志 | `docker logs cx-dzpk`(json-file,50MB×3 自动轮转) |

## 二、部署步骤(Windows 本地)

```powershell
# 1. 打包
cd E:\cocos\chexuan-dzpk
& "D:\cid\IntelliJ IDEA 2025.3.2\plugins\maven\lib\maven3\bin\mvn.cmd" clean package -DskipTests

# 2. 上传(密码见凭据文件,单引号包裹)
pscp -pw '<密码>' -hostkey "ssh-ed25519 255 SHA256:14GGJO42UVPRtNBHullNW9IBLxTCvnF1e3XFMqj8ap0" `
  target\chexuan-dzpk-1.0.0.jar root@47.122.115.33:/home/fzcx/cx-dzpk/cx-dzpk.jar
# Dockerfile/docker-compose.yml 变了也要一并传

# 3. 服务器上重建重启(独立 compose,不碰扯旋)
plink ... root@47.122.115.33 "cd /home/fzcx/cx-dzpk && DOCKER_BUILDKIT=0 docker compose build && docker compose up -d"

# 4. 验证
plink ... "docker ps --filter name=cx-dzpk --format '{{.Names}} {{.Status}}' && curl -s -o /dev/null -w '%{http_code}' http://127.0.0.1:19100/index.html"
# 期望: Up + 200

# 5. 清理构建垃圾(扯旋文档血泪教训:不清会写满 40G 根分区)
plink ... "docker builder prune -f && docker image prune -f"
```

## 三、踩过的坑

1. **`openjdk:17-jdk-slim` 拉不动**:服务器在国内,Docker Hub 拉取会无限挂起。
   基础镜像必须用服务器上已有的(`docker images` 确认),当前用 `eclipse-temurin:17-jre-alpine`。
2. **公网访问要开安全组**:本机 firewalld inactive、iptables 全放行,访问不通就是**阿里云安全组**没放行 TCP 19100。
3. **alpine 没有 curl**:健康检查用 `wget -q -O /dev/null`。
4. 磁盘 40G,每次 build 后固定跑 `docker builder prune -f`(构建缓存是大头)+ `docker image prune -f`。

## 四、测试(机器人陪打)

浏览器打开 `http://47.122.115.33:19100/`:

1. 连接 → 游客登录(填昵称)
2. 创建房间(盲注/结算时间/抽水都可调)→ 进房 → 坐下 → 带入
3. 几秒后 2 个"·AI"机器人自动进场坐下带入,牌局自动循环开局
4. 轮到你时点 弃牌/过牌/跟注/加注/全下;超时自动过牌/弃牌
5. 打满结算时间触发周期结算(抽水→退筹→补带入面板);你离开后机器人自动撤场、房间销毁

机器人参数在 `application.yml` `dzpk.robot.*`(数量/行动延迟),改了要重新打包部署。
