# 德州(cx-dzpk) × 扯旋(chexuan-springboot) 边缘功能对照

> 结论先行:除牌型规则不同外,房间/座位/结算/俱乐部的边缘行为向扯旋 1:1 看齐。
> 本表列出扯旋的真实实现(源码核对)、德州现状、以及本次补齐方案。
> 扯旋消息号写在「扯旋」列,德州消息号写在「德州」列(德州独立 4xx 段)。

## 一、功能对照总表

| # | 功能 | 扯旋行为(真值) | 德州现状 | 本次动作 |
|---|------|----------------|----------|----------|
| 1 | **留坐离桌/放假(grace)** | C→S 122 暂离 / 123 回桌;S→C 282。状态 `ON_LEAVE`,倒计时 **300s**(`seat_reserve_grace_seconds`);**每结算周期只能用 1 次**(`seatReserveUsed`,周期结算清零);放假中不发牌、不计入开局人数、周期计时关闸;牌局中未弃牌先记 `manualLeavePending`,弃牌/局末再进;重连**不会**自动回桌,必须发 123 | 只有 `sittingOut` 字段+开局过滤,无入口 | **补齐**:413/414 ↔ 474;grace 300s、周期一次、闸门、pending |
| 2 | 放假超时 → 物理锁座 | 超时先(如到期)周期结算再站起,座位号锁 **480s**(`seat_reserve_seconds`,281)只留给本人 | 无 | **补齐**:站起+锁座,`seatLocks` 进快照,他人坐下被拒 |
| 3 | **赢家逃跑** | 无硬锁,是**罚金**:①赢家早退过路费=盈利×**30%**(`winner_early_leave_rate`),仅俱乐部房、主动站起/离开、盈利>0、桌上还有别人、非群主;归群主。②逃跑罚金(`run_away_enabled` 默认**关**):累计离座≥**6分钟**且盈利≥阈值(缺省 2BB 基准)再扣 30%。离开前先回执 **code=92** 报罚金,前端带 `confirmFine:true` 重发才真扣;S→C 284 | 无 | **补齐**:STAND_UP 加 `confirmFine`;STAND_UP_RES status=92 报价;罚金进群主德州钱包;S→C 476 |
| 4 | **断线** | 断线**不踢座**:标离线(208)+`vacationPending`;轮到行动等操作超时自动休/弃,弃完自动进放假(本周期没用过)否则站起;重连推 207+快照,**不自动回桌**;无固定 60s 踢人 | 断线仅 unbind,超时代打已有;offline 字段从未置位、无广播 | **补齐**:断线置 offline+`vacationPending` → 广播 477;代弃后自动进放假;重连(再进房)广播 478 并清 pending |
| 5 | **周期到期结算+补入** | 每把结束查有效时长≥settleTime → 抽水→退积分→清桌→**不离座**→ 287 补带入等待 **10s**(`period_settle_bringin_seconds`),117 补入续打,超时 206 站起;周期结算时 `seatReserveUsed` 清零 | **已有**(结构一致):PERIOD_SETTLE(468)+BUY_IN(407),超时站起;等待默认 30s | **对齐**:等待默认 30s→**10s**;`resetPeriod` 顺带清暂离次数 |
| 6 | **实时战绩** | C→S 109,在房间就能查(观众也行):每人带入/当前/实时盈亏(未结手把桌面投入加回)/手数胜负平/剩余周期时间/暂离补入状态 + 房间总带入/池子 + 同 session 最近 50 条结算记录 | 只有历史战绩(411)与局末推送,无局中实时 | **补齐**:415 ↔ 475,含在座实时盈亏+房间汇总+本房最近结算记录 |
| 7 | **圈主扣钻石(周期服务费)** | 扣**群主**:本周期第一把发牌**预付** + 房间游戏时长到点**即扣**;金额按(settleTime,底分)档位,兜底 `commission_settle_diamond_cost=5`;扣不动 → 283 警告+局末全员强制站起;坐下前校验群主钻石够下期 | 只有建房扣**创建者**(默认 0),无周期扣、无警告 | **补齐**:开手时预付/到点扣群主钻;不足 → 488 警告+不开新局;坐下校验群主余额 |
| 8 | 建房扣钻(创建者) | 扣创建者,普通桌 20000/联盟桌 50000,**流局不退** | 已有(`create-room-diamond-cost`,默认 0,不退) | 保持;金额走配置 |
| 9 | **俱乐部桌权限** | 仅成员可**坐下**;**观战不校验成员**;**群主和管理员不能坐下玩**(2026-07-25);冻结/积分冻结拦坐下 | 进房就拦非成员;坐下无俱乐部校验 | **对齐**:进房放开(可观战);坐下校验成员+拦群主/管理员 |
| 10 | **解散牌局** | C→S 130,创建者**或**群主/管理员可解散 → 285 广播,全员结算退分 | 无(只有解散俱乐部) | **补齐**:416 ↔ 479;局中解散本手投入退回,再全员结算站起,房间销毁 |
| 11 | 带入审批(`requestToBringIn`) | 俱乐部房可开,带入需房主同意 | 无 | **暂不做**(建房参数已预留位,后续按需) |
| 12 | GPS 限制 | 建房参数有,坐下校验距离 | 参数已收(`gpsLimitOn`),逻辑未实现 | **暂不做**(客户端无定位上报链路) |

## 二、消息号映射(德州侧新增)

| 方向 | 德州 | 含义 | 对应扯旋 |
|------|------|------|----------|
| C→S | 413 SEAT_RESERVE_LEAVE | 留座暂离(放假) | 122 |
| C→S | 414 SEAT_RESERVE_RESUME | 回到座位 | 123 |
| C→S | 415 REALTIME_STATS | 实时战绩 | 109 |
| C→S | 416 DISMISS_ROOM | 解散牌局 | 130 |
| C→S | 408 STAND_UP `{confirmFine}` | 站起(罚金确认) | 104+confirmFine |
| S→C | 474 SEAT_RESERVE_GRACE | 放假状态 {state,deadline,reason,used} | 282(+281 锁座并入 state=SEAT_LOCKED) |
| S→C | 475 REALTIME_STATS_RES | 实时战绩数据 | 109 ack |
| S→C | 476 RUN_AWAY_FINE | 罚金 {kind:EARLY_LEAVE/RUN_AWAY} | 284 |
| S→C | 477 PLAYER_OFFLINE | 玩家断线 | 208 |
| S→C | 478 PLAYER_ONLINE | 玩家回线 | 207 |
| S→C | 479 ROOM_DISMISSED | 牌局被解散 | 285 |
| S→C | 488 DIAMOND_WARNING | 群主钻石不足警告 | 283 |
| S→C | 470 STAND_UP_RES `{status:92,fine}` | 站起罚金报价 | ack code=92 |

## 三、常量对照

| 配置 | 扯旋默认 | 德州配置项(默认) |
|------|----------|------------------|
| 放假倒计时 | 300s | `dzpk.seat-reserve-grace-secs: 300` |
| 站起后锁座 | 480s | `dzpk.seat-lock-secs: 480` |
| 周期补带入窗口 | 10s | `dzpk.await-buyin-secs: 10`(原 30 改) |
| 赢家早退费率 | 30% | `dzpk.winner-early-leave-rate: 30` |
| 逃跑罚金开关 | false | `dzpk.run-away-enabled: false` |
| 逃跑累计离座 | 6 分钟 | `dzpk.run-away-time-mins: 6` |
| 逃跑罚金率 | 30% | `dzpk.run-away-penalty-rate: 30` |
| 周期扣钻兜底 | 5 | `dzpk.owner-period-diamond-cost: 5` |
| 建房扣钻 | 20000/50000 | `dzpk.create-room-diamond-cost`(生产自配) |

## 四、经济归属差异说明

- 扯旋罚金/抽水进群主 **俱乐部积分**(ClubMember.score);德州没有独立积分体系,罚金与抽水分成统一进 **德州钱包**(`dz_user_wallet`),流水在 `dz_commission_log`/结算记录可查。
- 钻石是全平台公用(主库 `user.diamond`),德州侧只记 `dz_diamond_log` 流水;机器人/游客(id≥8亿)无主服账号,钻石相关逻辑自动跳过(便于开发环境跑通)。

## 五、前端匹配(cx-dzpk-pixi)

| 功能 | 界面 |
|------|------|
| 暂离/回桌 | 桌内菜单「留座暂离」;座位盖「暂离」标记+倒计时;自己暂离时中央「回到座位」按钮 |
| 断线标记 | 座位「离线」灰标(477/478 驱动) |
| 罚金确认 | 站起返回 status=92 → 弹窗展示罚金明细,确认后带 confirmFine 重发 |
| 实时战绩 | 桌内菜单「实时战绩」→ 面板:每人带入/盈亏/手数 + 房间汇总 |
| 解散牌局 | 桌内菜单「解散牌局」(创建者/群主/管理员可见)→ 确认 → 全员回大厅 |
| 钻石警告 | 收到 488 → 顶部红条提示 |
